langgraph>=0.2.0
langchain-core>=0.3.0
langchain>=0.3           # нужен для langfuse.langchain.CallbackHandler
pydantic>=2.7
sqlalchemy>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
langfuse>=3              # v3/v4: интеграция через langfuse.langchain
openai>=2                # боевой vision/LLM-режим; >=2 — нужен chat.completions.parse
aiogram>=3.4
python-dotenv>=1.0
psycopg[binary]>=3.1
# Для боевого режима также по выбору:
#   клиент облачного vision/LLM-API (structured output через pydantic-ai)
#   qdrant-client  ИЛИ  pgvector   (векторный индекс каталога)
#   pillow  (сборка коллажа образа)
